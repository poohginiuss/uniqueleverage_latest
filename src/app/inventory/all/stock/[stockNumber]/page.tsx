import { InventoryAllPageContent } from "../../page";

export default function StockNumberPage() {
  // Render the same content as the main inventory page
  // The InventoryAllPageContent component will handle the stockNumber from the URL
  return <InventoryAllPageContent />;
}
